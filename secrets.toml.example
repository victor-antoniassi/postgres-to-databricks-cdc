# Este arquivo é um exemplo de como configurar as credenciais para o pipeline `dlt`
# Para uso local, copie este conteúdo para `.dlt/secrets.toml` e preencha com suas credenciais.
# NÃO FAÇA COMMIT de `.dlt/secrets.toml` para o controle de versão!

[sources.pg_replication.credentials]
database = "your_db"
password = "your_password"
host = "your_host"
port = 5432
username = "your_user"

# Para o Databricks, se estiver usando um Token de Acesso Pessoal (PAT) ou Service Principal.
# Se você tiver o Databricks CLI configurado, o `dlt` pode tentar usar o perfil `DEFAULT`
# automaticamente sem precisar dessas credenciais aqui.
[destination.databricks.credentials]
server_hostname = "dbc-xxxx.cloud.databricks.com" # Ex: dbc-abcdefgh-ijkl.cloud.databricks.com
http_path = "/sql/1.0/warehouses/xxxx"             # Ex: /sql/1.0/warehouses/1234567890abcdef
access_token = "dapi..."                           # Token de acesso do Databricks (dapi...)
